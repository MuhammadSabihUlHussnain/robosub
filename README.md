# robosub

Static HTML and CSS site for the Southern Miss RoboSub Team.

## Local Hosting

This site does not use JavaScript. To host it locally, run a static file server from this directory:

```powershell
py -m http.server 8001 --bind 127.0.0.1
```

Then open:

```text
http://127.0.0.1:8001/
```
